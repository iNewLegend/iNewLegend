import type { IZConfig } from "@zenflux/cli";

const config: IZConfig = {
    format: [ "es" ],
    inputPath: "src/index.ts",
    outputName: "@inewlegend/website",
    outputFileName: "website",
};

export default config;