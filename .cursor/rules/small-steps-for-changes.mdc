---
description: For every change use small steps
globs: *.tsx,*ts
alwaysApply: true
---

# Use Small steps/chunks for editing

- Each edit request should be in as small chunks/steps as possible
- Every request that requires changes should be split into small chunks
- For each step we test before we proceed to the next